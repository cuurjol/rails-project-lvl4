---
en:
  pundit:
    default: You are not authorized to perform this action
    repository_policy:
      index?: You don't have permissions to view a list of repositories
      new?: You don't have permissions to create a new repository
      create?: You don't have permissions to create a new repository
      show?: You don't have permissions to view this repository
      destroy?: You don't have permissions to destroy this repository
